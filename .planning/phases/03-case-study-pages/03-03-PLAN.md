---
phase: 03-case-study-pages
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/diagrams/GleeMeetDiagram.tsx
  - components/diagrams/CareerBoxDiagram.tsx
  - components/diagrams/ZorovaDiagram.tsx
autonomous: true
requirements:
  - CASE-06

must_haves:
  truths:
    - "GleeMeet diagram shows system architecture with Client, API, WebSocket, Cache (Redis), Search (OpenSearch), Database (DynamoDB), and BERT ML layers"
    - "CareerBox diagram shows system architecture with Client, API, Cache (Redis), Search (OpenSearch), Database (MongoDB) layers"
    - "Zorova diagram shows marketplace architecture with Consumer App, Provider App, API, Cache (Redis), Search (OpenSearch), Database (MongoDB) layers"
    - "All diagrams use 800x400 viewBox with responsive width/height"
    - "Accent nodes (Redis, OpenSearch, primary DB) use project-specific accent color stroke"
    - "All diagrams have aria-label for accessibility"
  artifacts:
    - path: "components/diagrams/GleeMeetDiagram.tsx"
      provides: "GleeMeet system architecture SVG (most complex — 7-8 nodes including BERT sidecar)"
      exports: ["default"]
    - path: "components/diagrams/CareerBoxDiagram.tsx"
      provides: "CareerBox system architecture SVG (5-6 nodes)"
      exports: ["default"]
    - path: "components/diagrams/ZorovaDiagram.tsx"
      provides: "Zorova marketplace architecture SVG (6-7 nodes including dual client apps)"
      exports: ["default"]
  key_links:
    - from: "components/diagrams/GleeMeetDiagram.tsx"
      to: "app/case-studies/[slug]/page.tsx"
      via: "diagramMap lookup in Plan 04"
      pattern: "GleeMeetDiagram"
    - from: "components/diagrams/CareerBoxDiagram.tsx"
      to: "app/case-studies/[slug]/page.tsx"
      via: "diagramMap lookup in Plan 04"
      pattern: "CareerBoxDiagram"
    - from: "components/diagrams/ZorovaDiagram.tsx"
      to: "app/case-studies/[slug]/page.tsx"
      via: "diagramMap lookup in Plan 04"
      pattern: "ZorovaDiagram"
---

<objective>
Create inline React SVG architecture diagrams for all 3 case studies.

Purpose: Architecture diagrams are the visual centerpiece of each case study (Section 03 per D-02). They demonstrate system design thinking to CTOs evaluating the studio. Per D-06: 5-8 boxes showing major layers with data flow arrows. Static SVG in Phase 3 (animations deferred to Phase 4 ANIM-03).

Output: 3 diagram components in `components/diagrams/` — Server Components with no GSAP dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-case-study-pages/03-CONTEXT.md
@.planning/phases/03-case-study-pages/03-RESEARCH.md
@.planning/phases/03-case-study-pages/03-UI-SPEC.md

@data/case-studies.ts
@data/projects.ts
@config/design-system.ts

@.planning/phases/03-case-study-pages/03-01-SUMMARY.md

<interfaces>
<!-- UI-SPEC Architecture Diagram visual rules: -->
- Canvas: viewBox="0 0 800 400", width="100%" height="auto" — responsive
- Background: transparent (parent card provides bg)
- Node boxes: 120x48px rounded-rect (rx="8"), fill var(--bg), stroke var(--line), stroke-width 1.5
- Node text: fontFamily="var(--font-mono), JetBrains Mono, monospace", 11px, fill var(--text-secondary), text-anchor middle
- Accent nodes (Redis, OpenSearch, primary DB): stroke var(--{project.accent}), fill rgba({accent-rgb},0.08)
- Arrows: stroke var(--line-light), stroke-width 1, marker-end arrowhead
- Layer labels: 9px uppercase, fill var(--text-muted), fontFamily="var(--font-mono), JetBrains Mono, monospace"
- aria-label="System architecture diagram for {project.title}" on <svg> element
- role="img" on <svg> element

<!-- Project accent colors (hex for SVG fill rgba): -->
- GleeMeet (pink): var(--pink) = #ec4899, rgba fill = rgba(236,72,153,0.08)
- CareerBox (violet): var(--violet) = #a855f7, rgba fill = rgba(168,85,247,0.08)
- Zorova (orange): var(--orange) = #f97316, rgba fill = rgba(249,115,22,0.08)

<!-- Marker IDs must be unique per diagram to prevent SVG defs collisions: -->
- GleeMeetDiagram: "arrowhead-glee"
- CareerBoxDiagram: "arrowhead-career"
- ZorovaDiagram: "arrowhead-zorova"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GleeMeet architecture diagram (reference implementation)</name>
  <files>components/diagrams/GleeMeetDiagram.tsx</files>
  <read_first>
    - data/case-studies.ts (GleeMeet case study content — understand the architecture being diagrammed)
    - .planning/phases/03-case-study-pages/03-RESEARCH.md (Pattern 3: Inline React SVG Diagrams — exact code pattern, visual rules, font-family approach)
    - .planning/phases/03-case-study-pages/03-UI-SPEC.md (Architecture Diagram Specification — all visual rules)
    - config/design-system.ts (color values for reference)
  </read_first>
  <action>
Create `components/diagrams/GleeMeetDiagram.tsx` as the reference diagram. This is the most complex diagram (7-8 nodes) and establishes the pattern for the other two.

**Create the directory first** if it doesn't exist: `components/diagrams/`

**Component structure:**
```typescript
export default function GleeMeetDiagram() {
  return (
    <svg
      viewBox="0 0 800 400"
      width="100%"
      height="auto"
      aria-label="System architecture diagram for GleeMeet"
      role="img"
      className="block"
    >
      <defs>
        <marker id="arrowhead-glee" markerWidth="8" markerHeight="6"
                refX="8" refY="3" orient="auto">
          <polygon points="0 0, 8 3, 0 6" fill="var(--line-light)" />
        </marker>
      </defs>
      {/* Layer labels, nodes, arrows */}
    </svg>
  );
}
```

**GleeMeet Architecture (7-8 nodes):**
The system has these layers flowing left-to-right:

Layer layout (approximate x positions):
- CLIENT layer (x ~60): Next.js + Redux (1 node)
- API layer (x ~220): Node.js / Express (1 node), WebSocket Server (1 node, below)
- CACHE layer (x ~400): Redis (accent node)
- SEARCH layer (x ~540): OpenSearch (accent node)
- DATABASE layer (x ~680): DynamoDB (accent node)
- ML layer (positioned below main flow): BERT Matching (1 node, connected to API and DynamoDB)

Approximate y positions:
- Layer labels at y=20
- Main row nodes at y=60 (centered in row, 48px tall)
- Second row nodes at y=170 (WebSocket, BERT)
- Total fits within 400px height

**Node specifications:**
Standard nodes (Next.js+Redux, Node.js/Express, WebSocket Server):
- `<rect>` with `fill="var(--bg)" stroke="var(--line)" strokeWidth={1.5} rx={8}` width={120} height={48}
- `<text>` centered: `fontSize={11} fill="var(--text-secondary)" fontFamily="var(--font-mono), JetBrains Mono, monospace" textAnchor="middle"`

Accent nodes (Redis, OpenSearch, DynamoDB):
- `<rect>` with `fill="rgba(236,72,153,0.08)" stroke="var(--pink)" strokeWidth={1.5} rx={8}` width={120} height={48}
- Same text styling as standard nodes

BERT node (special — ML sidecar):
- Standard node styling (not accent — BERT is not part of the Redis+OpenSearch+DB trio)

**Arrows between nodes:**
Use `<line>` elements with `stroke="var(--line-light)" strokeWidth={1} markerEnd="url(#arrowhead-glee)"`.

Data flow connections:
1. Next.js → Node.js/Express (client to API)
2. Next.js → WebSocket Server (real-time channel)
3. Node.js/Express → Redis (cache reads/writes)
4. Node.js/Express → OpenSearch (search queries)
5. Node.js/Express → DynamoDB (data persistence)
6. Node.js/Express → BERT (matching requests)
7. BERT → DynamoDB (vector reads)
8. Redis → WebSocket Server (pub/sub for real-time)

**Layer labels** positioned above each column:
`<text>` with `fontSize={9} fill="var(--text-muted)" fontFamily="var(--font-mono), JetBrains Mono, monospace" textAnchor="middle"` and CSS `textTransform` via style attribute: `style={{ textTransform: 'uppercase' }}`

Labels: "CLIENT", "API", "CACHE", "SEARCH", "DATABASE", "ML"

**This is a Server Component** — NO 'use client' directive, NO GSAP imports, NO animations. Pure static SVG.
  </action>
  <verify>
    <automated>cd D:/portfolio/companyportfolio && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - components/diagrams/GleeMeetDiagram.tsx exists
    - File exports a default function `GleeMeetDiagram`
    - SVG has `viewBox="0 0 800 400"`
    - SVG has `width="100%"` and `height="auto"`
    - SVG has `aria-label="System architecture diagram for GleeMeet"`
    - SVG has `role="img"`
    - File contains `id="arrowhead-glee"` (unique marker ID)
    - File contains at least 6 `<rect` elements (6+ nodes)
    - File contains `fill="rgba(236,72,153,0.08)"` (pink accent fill for Redis/OpenSearch/DynamoDB)
    - File contains `stroke="var(--pink)"` (pink accent stroke)
    - File contains `fontFamily` referencing `var(--font-mono)` or `JetBrains Mono`
    - File contains text labels: "Next.js", "Redis", "OpenSearch", "DynamoDB"
    - File does NOT contain `'use client'`
    - File does NOT import from `@/lib/gsap` or `gsap`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>GleeMeet architecture diagram renders as static inline SVG with 7-8 nodes showing Client, API, WebSocket, Cache, Search, Database, and ML layers. Accent nodes (Redis, OpenSearch, DynamoDB) use pink stroke. Arrows show data flow. Responsive and accessible.</done>
</task>

<task type="auto">
  <name>Task 2: Create CareerBox and Zorova architecture diagrams</name>
  <files>components/diagrams/CareerBoxDiagram.tsx, components/diagrams/ZorovaDiagram.tsx</files>
  <read_first>
    - components/diagrams/GleeMeetDiagram.tsx (Task 1 output — reference pattern for node layout, styling, arrow patterns)
    - data/case-studies.ts (CareerBox and Zorova case study content — understand architectures being diagrammed)
    - .planning/phases/03-case-study-pages/03-UI-SPEC.md (Architecture Diagram Specification)
  </read_first>
  <action>
Create 2 more diagram components following the GleeMeet reference pattern established in Task 1.

**CareerBox Diagram (`components/diagrams/CareerBoxDiagram.tsx`):**

5-6 nodes. Simpler than GleeMeet (no WebSocket, no ML layer).

Architecture layers:
- CLIENT: Next.js (SSR) — 1 node
- API: Node.js / Express — 1 node
- CACHE: Redis — accent node (violet)
- SEARCH: OpenSearch — accent node (violet)
- DATABASE: MongoDB — accent node (violet)

Data flow:
1. Next.js → Node.js/Express (SSR + client requests)
2. Node.js/Express → Redis (session cache, job listing cache)
3. Node.js/Express → OpenSearch (job search queries)
4. Node.js/Express → MongoDB (data persistence)
5. Redis ← → Node.js/Express (bidirectional for session management)

Accent color: violet `var(--violet)`, fill `rgba(168,85,247,0.08)`
Marker ID: `arrowhead-career`
aria-label: `"System architecture diagram for CareerBox"`

Same viewBox (800x400), responsive sizing, Server Component, no 'use client', no GSAP.

**Zorova Diagram (`components/diagrams/ZorovaDiagram.tsx`):**

6-7 nodes. Marketplace = two client apps.

Architecture layers:
- CLIENT: Consumer App (React.js) — 1 node, Provider App (React.js) — 1 node (stacked vertically or side by side)
- API: Node.js / Express — 1 node (central, receives from both clients)
- CACHE: Redis — accent node (orange)
- SEARCH: OpenSearch — accent node (orange)
- DATABASE: MongoDB — accent node (orange)

Data flow:
1. Consumer App → Node.js/Express
2. Provider App → Node.js/Express
3. Node.js/Express → Redis (availability cache, booking slots)
4. Node.js/Express → OpenSearch (service discovery)
5. Node.js/Express → MongoDB (bookings, providers, consumers)

Accent color: orange `var(--orange)`, fill `rgba(249,115,22,0.08)`
Marker ID: `arrowhead-zorova`
aria-label: `"System architecture diagram for Zorova"`

Same viewBox (800x400), responsive sizing, Server Component, no 'use client', no GSAP.

**Both diagrams follow the EXACT same code structure as GleeMeetDiagram.tsx:**
- Default export function
- SVG with viewBox, width, height, aria-label, role
- defs with unique marker
- Layer labels in uppercase dev-mono 9px
- Standard nodes with var(--bg) fill, var(--line) stroke
- Accent nodes with rgba fill, var(--{accent}) stroke
- Arrows with var(--line-light) stroke and markerEnd
  </action>
  <verify>
    <automated>cd D:/portfolio/companyportfolio && npx tsc --noEmit 2>&1 | head -10 && echo "---" && ls components/diagrams/</automated>
  </verify>
  <acceptance_criteria>
    - components/diagrams/CareerBoxDiagram.tsx exists and exports default function `CareerBoxDiagram`
    - components/diagrams/ZorovaDiagram.tsx exists and exports default function `ZorovaDiagram`
    - CareerBoxDiagram has `viewBox="0 0 800 400"`, `aria-label="System architecture diagram for CareerBox"`, `role="img"`
    - ZorovaDiagram has `viewBox="0 0 800 400"`, `aria-label="System architecture diagram for Zorova"`, `role="img"`
    - CareerBoxDiagram contains `id="arrowhead-career"` (unique marker)
    - ZorovaDiagram contains `id="arrowhead-zorova"` (unique marker)
    - CareerBoxDiagram contains `stroke="var(--violet)"` and `fill="rgba(168,85,247,0.08)"` (violet accent)
    - ZorovaDiagram contains `stroke="var(--orange)"` and `fill="rgba(249,115,22,0.08)"` (orange accent)
    - CareerBoxDiagram contains text "MongoDB" (not DynamoDB)
    - ZorovaDiagram contains text "Consumer App" or similar dual-client label
    - ZorovaDiagram contains text "Provider App" or similar
    - Neither file contains `'use client'`
    - Neither file imports from `@/lib/gsap` or `gsap`
    - `npx tsc --noEmit` passes
    - 3 files total in components/diagrams/ directory
  </acceptance_criteria>
  <done>All 3 architecture diagrams are complete: GleeMeet (7-8 nodes, pink), CareerBox (5-6 nodes, violet), Zorova (6-7 nodes, orange). All are static Server Components with responsive SVG, unique marker IDs, and accessibility labels. Ready for import in the detail page template (Plan 04).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `ls components/diagrams/` shows 3 .tsx files
- Each diagram has unique marker IDs (no collision risk)
- Each diagram uses correct project accent color
- No diagram imports GSAP or uses 'use client'
- All diagrams have aria-label and role="img"
</verification>

<success_criteria>
Three architecture SVG diagram components exist in components/diagrams/, each showing the system design for its respective project. Diagrams are static, accessible, responsive, and ready for import in the detail page template.
</success_criteria>

<output>
After completion, create `.planning/phases/03-case-study-pages/03-03-SUMMARY.md`
</output>
