---
phase: 06-studio-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Methodology.tsx
  - app/page.tsx
autonomous: true
requirements:
  - STUD-05

must_haves:
  truths:
    - "A #methodology section exists on the landing page explaining the MVP-first approach in 4 steps"
    - "The 4 steps are: Discover, Build MVP, Iterate, Scale -- each with a numbered label and description"
    - "Navigation scroll-spy activates 'How We Work' nav item when the methodology section is in view"
    - "Methodology section appears between Experience and Projects in page order"
  artifacts:
    - path: "components/Methodology.tsx"
      provides: "4-step methodology section with scroll-triggered animations"
      contains: "id=\"methodology\""
      min_lines: 80
    - path: "app/page.tsx"
      provides: "Page composition with Methodology inserted"
      contains: "Methodology"
  key_links:
    - from: "app/page.tsx"
      to: "components/Methodology.tsx"
      via: "import and JSX rendering"
      pattern: "import Methodology from '@/components/Methodology'"
    - from: "components/Navigation.tsx"
      to: "components/Methodology.tsx"
      via: "scroll-spy finds element with id=methodology"
      pattern: "id=\"methodology\""
---

<objective>
Create a new Methodology.tsx component for the "How we work" section and insert it into app/page.tsx between Experience and Projects. The section displays 4 numbered typographic steps (Discover, Build MVP, Iterate, Scale) explaining the studio's MVP-first development process.

Purpose: Completes STUD-05 (methodology section). Also enables the Navigation scroll-spy for the "How We Work" nav item by creating a section with `id="methodology"` in the DOM.

Output: New components/Methodology.tsx + updated app/page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-studio-sections/06-CONTEXT.md
@.planning/phases/06-studio-sections/06-UI-SPEC.md
@.planning/phases/06-studio-sections/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/gsap.ts:
```typescript
export { gsap, ScrollTrigger, SplitText, DrawSVGPlugin, useGSAP };
```

From app/page.tsx (current state):
```typescript
import Hero from '@/components/Hero';
import About from '@/components/About';
import Experience from '@/components/Experience';
import Projects from '@/components/Projects';
import Contact from '@/components/Contact';
import Navigation from '@/components/Navigation';
import Footer from '@/components/Footer';

export default function Home() {
  return (
    <>
      <Navigation />
      <main id="main-content" className="relative">
        <Hero />
        <About />
        <Experience />
        <Projects />
        <Contact />
      </main>
      <Footer />
    </>
  );
}
```

From components/Navigation.tsx (scroll-spy array, line 32):
```typescript
const ids = ['contact', 'projects', 'methodology', 'experience', 'team'];
```

From components/Navigation.tsx (nav items, lines 56-63):
```typescript
const items = [
  { name: 'Team', href: '#team' },
  { name: 'Journey', href: '#experience' },
  { name: 'How We Work', href: '#methodology' },
  { name: 'Projects', href: '#projects' },
  { name: 'Case Studies', href: '/case-studies' },
  { name: 'Contact', href: '#contact' },
];
```

From app/globals.css (relevant utilities):
```css
.section-line { height: 1px; background: var(--line); }
/* CSS vars: --violet: #a855f7; --text: #fafafa; --text-secondary: #a1a1aa; --text-muted: #52525b */
```

Section structure pattern (from Experience.tsx):
```html
<section id="[id]" ref={sectionRef} aria-label="[label]" className="py-32 lg:py-44 content-auto">
  <div className="section-line" aria-hidden="true" />
  <div className="max-w-[1400px] mx-auto px-6 lg:px-16 xl:px-24 pt-32">
    <span className="section-label dev-mono text-xs text-[var(--violet)] tracking-[0.25em] uppercase block mb-8">...</span>
    <h2 className="section-heading heading-lg text-white mb-12">...</h2>
    {/* content */}
  </div>
</section>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Methodology.tsx -- 4-step process section</name>
  <files>components/Methodology.tsx</files>
  <read_first>
    - components/Experience.tsx (reference pattern for section structure, useGSAP + gsap.matchMedia animation approach)
    - lib/gsap.ts (import path -- must use @/lib/gsap, never import gsap directly)
    - app/globals.css (section-line class, CSS custom properties)
    - config/design-system.ts (color token names)
    - .planning/phases/06-studio-sections/06-UI-SPEC.md (exact copy, animation specs, typography tiers)
  </read_first>
  <action>
    Create new file components/Methodology.tsx as a client component with scroll-triggered animations.

    **FILE STRUCTURE:**
    ```typescript
    'use client';

    import { useRef } from 'react';
    import { gsap, useGSAP } from '@/lib/gsap';
    ```

    **STEP DATA (per D-11, UI-SPEC Copywriting Contract):**
    Define steps array inline (no external data file needed -- only 4 static items):
    ```typescript
    const steps = [
      {
        num: '01 /',
        title: 'Discover',
        body: 'Two weeks of questions before a single line of code. We map your domain, users, and constraints into a technical spec you can read.',
      },
      {
        num: '02 /',
        title: 'Build MVP',
        body: 'Ship the smallest version that proves the idea. Real tech, real users \u2014 no mock-ups masquerading as products.',
      },
      {
        num: '03 /',
        title: 'Iterate',
        body: 'Weekly cycles. You review, we refine. Every feedback loop tightens the product toward what actually works.',
      },
      {
        num: '04 /',
        title: 'Scale',
        body: 'Once the pattern is proven, we harden it \u2014 caching layers, optimized queries, infrastructure that grows with demand.',
      },
    ];
    ```

    **COMPONENT:**
    ```typescript
    const Methodology = () => {
      const sectionRef = useRef<HTMLElement>(null);

      // Animation block (see below)

      return (
        <section id="methodology" ref={sectionRef} aria-label="How we work" className="py-32 lg:py-44 content-auto">
          <div className="section-line" aria-hidden="true" />
          <div className="max-w-[1400px] mx-auto px-6 lg:px-16 xl:px-24 pt-32">
            <span className="section-label dev-mono text-xs text-[var(--violet)] tracking-[0.25em] uppercase block mb-8">
              04 / How We Work
            </span>
            <h2 className="section-heading heading-lg text-white mb-12">
              Our process.
            </h2>
            <div className="grid lg:grid-cols-4 gap-8 lg:gap-6">
              {steps.map((step) => (
                <div key={step.num} className="step-block">
                  <div className="dev-mono font-bold text-[var(--violet)] leading-none mb-3"
                       style={{ fontSize: 'clamp(2.5rem, 8vw, 5rem)' }}>
                    {step.num}
                  </div>
                  <h3 className="text-sm font-bold text-white leading-tight mb-2">
                    {step.title}
                  </h3>
                  <p className="text-sm text-[var(--text-secondary)]" style={{ lineHeight: 1.6 }}>
                    {step.body}
                  </p>
                </div>
              ))}
            </div>
          </div>
        </section>
      );
    };

    export default Methodology;
    ```

    **Section label:** `04 / How We Work` (position 04 in page flow per D-14: Hero, Team, Experience, How We Work, Projects, Contact). Caption tier: 12px, weight 400.

    **Section heading:** `Our process.` (per UI-SPEC Copywriting Contract). Uses existing `.heading-lg` class.

    **Grid layout (per D-12):**
    - Desktop (lg+): `grid-cols-4` with `gap-6` -- 4 equal columns, horizontal
    - Mobile: `grid-cols-1` with `gap-8` -- stacked, full-width
    - No connecting lines, no icons, no cards -- pure typographic blocks (per D-10)

    **Step block typography (per UI-SPEC):**
    - Number: Display tier `clamp(2.5rem, 8vw, 5rem)`, `.dev-mono`, `font-bold`, `text-[var(--violet)]`, `leading-none`, `mb-3`
    - Title: Body tier 14px (`text-sm`), `font-bold`, `text-white`, `leading-tight`, `mb-2`
    - Body: Body tier 14px (`text-sm`), weight 400, `text-[var(--text-secondary)]`, `lineHeight: 1.6`

    **ANIMATION BLOCK (useGSAP + gsap.matchMedia):**
    ```typescript
    useGSAP(() => {
      const section = sectionRef.current;
      if (!section) return;
      const mm = gsap.matchMedia();
      mm.add({
        isDesktop: '(min-width: 768px)',
        isMobile: '(max-width: 767px)',
        reduceMotion: '(prefers-reduced-motion: reduce)',
      }, (context) => {
        const { isMobile, reduceMotion } = context.conditions!;

        if (reduceMotion) {
          gsap.set(
            section.querySelectorAll('.section-label, .section-heading, .step-block'),
            { opacity: 1, y: 0, clearProps: 'all' }
          );
          return;
        }

        // Section label clip reveal
        const label = section.querySelector('.section-label');
        if (label) {
          gsap.fromTo(label,
            { clipPath: 'inset(0 100% 0 0)' },
            { clipPath: 'inset(0 0% 0 0)', duration: 1, ease: 'power3.inOut',
              scrollTrigger: { trigger: label, start: 'top 90%' } }
          );
        }

        // Heading slide up
        const heading = section.querySelector('.section-heading');
        if (heading) {
          gsap.from(heading, {
            y: isMobile ? 30 : 60, opacity: 0,
            duration: isMobile ? 0.4 : 1, ease: 'power3.out',
            scrollTrigger: { trigger: heading, start: 'top 85%' },
          });
        }

        // Step blocks stagger reveal
        section.querySelectorAll('.step-block').forEach((el, i) => {
          gsap.fromTo(el,
            { y: isMobile ? 20 : 40, opacity: 0 },
            { y: 0, opacity: 1,
              duration: isMobile ? 0.4 : 0.8, ease: 'power3.out',
              scrollTrigger: { trigger: el, start: 'top 80%' },
              delay: i * 0.15 }
          );
        });
      });
    }, { scope: sectionRef });
    ```

    **CRITICAL:** Always pass `{ scope: sectionRef }` to `useGSAP`. Never import gsap directly -- only from `@/lib/gsap`.
  </action>
  <verify>
    <automated>cd D:/portfolio/companyportfolio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - components/Methodology.tsx exists
    - components/Methodology.tsx contains `'use client'` directive
    - components/Methodology.tsx contains `id="methodology"`
    - components/Methodology.tsx contains `aria-label="How we work"`
    - components/Methodology.tsx contains `import { gsap, useGSAP } from '@/lib/gsap'` (not direct gsap import)
    - components/Methodology.tsx contains `04 / How We Work` (section label)
    - components/Methodology.tsx contains `Our process.` (section heading)
    - components/Methodology.tsx contains `Discover` and `Build MVP` and `Iterate` and `Scale` (4 step titles)
    - components/Methodology.tsx contains `step-block` class (GSAP selector target)
    - components/Methodology.tsx contains `gsap.matchMedia()` (responsive animation)
    - components/Methodology.tsx contains `reduceMotion` (accessibility branch)
    - components/Methodology.tsx contains `section-line` (visual separator)
    - components/Methodology.tsx contains `py-32 lg:py-44` (section padding matching other sections)
    - components/Methodology.tsx contains `pt-32` (inner wrapper padding)
    - components/Methodology.tsx contains `grid lg:grid-cols-4` (4-column desktop layout per D-12)
    - components/Methodology.tsx contains `text-[var(--violet)]` (accent color on step numbers per D-10)
    - components/Methodology.tsx contains `clamp(2.5rem, 8vw, 5rem)` (Display tier font size)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Methodology.tsx renders 4 numbered typographic steps in a horizontal grid (desktop) / stacked (mobile) with scroll-triggered stagger reveal animations and reduceMotion branch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Methodology into page.tsx between Experience and Projects</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current page composition -- must see exact import list and JSX order)
    - components/Methodology.tsx (just created in Task 1 -- confirm export name)
  </read_first>
  <action>
    Update app/page.tsx to import and render the Methodology component.

    **Add import (after Experience import, before Projects import):**
    ```typescript
    import Methodology from '@/components/Methodology';
    ```

    **Add JSX (between Experience and Projects in the main element):**
    The final page.tsx should read:
    ```typescript
    import Hero from '@/components/Hero';
    import About from '@/components/About';
    import Experience from '@/components/Experience';
    import Methodology from '@/components/Methodology';
    import Projects from '@/components/Projects';
    import Contact from '@/components/Contact';
    import Navigation from '@/components/Navigation';
    import Footer from '@/components/Footer';

    export default function Home() {
      return (
        <>
          <Navigation />
          <main id="main-content" className="relative">
            <Hero />
            <About />
            <Experience />
            <Methodology />
            <Projects />
            <Contact />
          </main>
          <Footer />
        </>
      );
    }
    ```

    Page order per D-14: Hero -> About/Team (#team) -> Experience (#experience) -> Methodology (#methodology) -> Projects (#projects) -> Contact (#contact).

    Do NOT change any other imports or JSX. Only add the Methodology import and JSX element.
  </action>
  <verify>
    <automated>cd D:/portfolio/companyportfolio && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx contains `import Methodology from '@/components/Methodology'`
    - app/page.tsx contains `<Methodology />` in JSX
    - app/page.tsx renders Methodology AFTER Experience and BEFORE Projects (grep for the sequence `Experience.*Methodology.*Projects` across lines)
    - app/page.tsx still contains all other existing imports (Hero, About, Experience, Projects, Contact, Navigation, Footer)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>
    Methodology component is rendered on the landing page between Experience and Projects. The #methodology anchor resolves in the DOM, enabling Navigation scroll-spy for "How We Work".
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. components/Methodology.tsx exists with `id="methodology"` and all 4 steps
4. app/page.tsx imports and renders Methodology between Experience and Projects
5. Grep confirms `id="methodology"` present in Methodology.tsx
6. Grep confirms Methodology import present in page.tsx
</verification>

<success_criteria>
- Methodology.tsx is a new client component with 4 numbered steps (Discover, Build MVP, Iterate, Scale)
- Section id="methodology" enables Navigation scroll-spy for "How We Work"
- Component inserted between Experience and Projects in page.tsx (per D-14 page flow)
- All animations use useGSAP + gsap.matchMedia with reduceMotion branch
- TypeScript compiles and build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-studio-sections/06-02-SUMMARY.md`
</output>
