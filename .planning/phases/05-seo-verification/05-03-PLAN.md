---
phase: 05-seo-verification
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified: []
autonomous: false
requirements:
  - SEO-06

must_haves:
  truths:
    - "npm run build completes without errors after all Phase 5 changes"
    - "Landing page loads and renders correctly in dev server"
    - "At least one case study page loads and renders correctly in dev server"
    - "OG images are accessible at /case-studies/{slug}/opengraph-image in dev server"
    - "Core Web Vitals are green (LCP < 2.5s, CLS < 0.1, INP < 200ms) or non-green scores have been remediated per D-11"
  artifacts: []
  key_links: []
---

<objective>
Build the project to validate all Phase 5 changes work together, then have the user measure Core Web Vitals and verify OG images render correctly. If CWV scores are not green, apply targeted fixes.

Purpose: Confirm that schema migration, OG image generation, URL fixes, image optimization changes, and robots.txt cleanup all integrate without regressions. CWV measurement requires a browser (Lighthouse in Chrome DevTools) which Claude cannot run. Per D-11, any non-green CWV scores must be fixed to certify SEO-06 complete.

Output: Successful build verification, user-confirmed CWV measurements, and any CWV remediation applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-verification/05-CONTEXT.md
@.planning/phases/05-seo-verification/05-RESEARCH.md

@.planning/phases/05-seo-verification/05-01-SUMMARY.md
@.planning/phases/05-seo-verification/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build verification, integration check, and CWV pre-flight</name>
  <files></files>
  <read_first>
    - .planning/phases/05-seo-verification/05-01-SUMMARY.md (what Plan 01 changed)
    - .planning/phases/05-seo-verification/05-02-SUMMARY.md (what Plan 02 changed)
    - app/layout.tsx (verify Organization JSON-LD is in place)
    - app/case-studies/[slug]/opengraph-image.tsx (verify OG image file exists)
    - next.config.js (verify unoptimized flag removed)
  </read_first>
  <action>
**Step 1: Run full Next.js build to validate everything compiles and generates correctly.**

```bash
npm run build
```

The build must complete with zero errors. It will:
- Type-check all TypeScript
- Generate static pages (including case study pages via `generateStaticParams`)
- Generate OG images via `opengraph-image.tsx` for each case study slug
- Validate JSON-LD can be serialized

**Step 2: If build fails, diagnose and fix the issue.**

Common failure modes after Phase 5 changes:
- TypeScript error in opengraph-image.tsx: Check that `params` is typed as `Promise<{ slug: string }>` and awaited
- Import path error: Verify `@/data/case-studies`, `@/data/projects` imports resolve correctly
- Font file not found: Verify `public/fonts/ttf/JetBrainsMono-SemiBold.ttf` exists at the path
- `next/og` import error: Verify `import { ImageResponse } from 'next/og'` (not `@vercel/og`)

**Step 3: Start dev server for user verification.**

```bash
npm run dev
```

Leave the dev server running for the checkpoint task. The user will need it to run Lighthouse and inspect OG images.

**Step 4: Quick automated checks on the build output.**

After `npm run build` succeeds:
- Check that `.next/server/app/case-studies/[slug]/opengraph-image` routes were generated
- Verify the build output lists the case study routes as static

**Step 5: CWV pre-flight checks (per D-11).**

Before the user runs Lighthouse, verify the following CWV-relevant factors are correct:

1. **LCP readiness:** Check that the hero section's primary visual element (likely a text heading or SVG) does not depend on a slow-loading resource. Verify that `next.config.js` no longer has `unoptimized: true`. If any `<Image>` component is above-the-fold, confirm it has the `priority` prop.
   ```bash
   grep -n "priority" components/Hero.tsx || echo "No priority prop found in Hero"
   ```

2. **CLS readiness:** Verify that any images in the codebase have explicit dimensions (width/height attributes or CSS sizing). Check that fonts are preloaded or use `next/font` (they are -- Inter and JetBrains Mono via `next/font/google`).
   ```bash
   grep -n "next/font" app/layout.tsx
   ```

3. **INP readiness:** Verify no render-blocking scripts in the `<head>`. Check that Google Tag Manager is loaded async (it is, via `dangerouslySetInnerHTML` in layout.tsx).

These checks help anticipate issues before the user runs Lighthouse. If any issues are found here, fix them proactively before the checkpoint.
  </action>
  <verify>
    <automated>cd D:/portfolio/companyportfolio && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - Build output shows `/case-studies/gleemeet`, `/case-studies/careerbox`, `/case-studies/zorova` as generated routes
    - No TypeScript errors in build output
    - No "Module not found" errors in build output
    - CWV pre-flight checks completed (font loading verified, no render-blocking scripts identified)
  </acceptance_criteria>
  <done>Next.js build passes with all Phase 5 changes integrated. CWV pre-flight checks complete. Dev server is running for user verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies CWV scores and OG images</name>
  <what-built>
All Phase 5 SEO changes are complete:
1. JSON-LD migrated to Organization with two-founder array
2. Dynamic OG images for all 3 case studies (accent-colored dark branded cards)
3. GleeMeet URL fixed, dead links (Zorova/Huslemad) rendered as non-clickable
4. Image optimization re-enabled (unoptimized flag removed)
5. Robots.txt cleaned of phantom disallow rules
6. Case study metadata extended with openGraph and twitter fields
  </what-built>
  <how-to-verify>
**OG Image Verification (SEO-03):**
1. Open `http://localhost:3000/case-studies/gleemeet/opengraph-image` in browser -- should show a 1200x630 PNG with dark background, pink accent edge, "GleeMeet" title, tech pills
2. Open `http://localhost:3000/case-studies/careerbox/opengraph-image` -- violet accent
3. Open `http://localhost:3000/case-studies/zorova/opengraph-image` -- orange accent

**JSON-LD Verification (SEO-01):**
4. Open `http://localhost:3000`, View Page Source, search for `application/ld+json` -- should contain `"@type":"Organization"` with a `"founder"` array of two Person objects

**Metadata Verification (SEO-02):**
5. Open `http://localhost:3000/case-studies/gleemeet`, View Page Source -- check for `og:title`, `og:description`, `og:image`, `twitter:card` meta tags

**Dead Link Verification (SEO-04):**
6. Open `http://localhost:3000`, scroll to Projects section -- GleeMeet should link to gleemeet.com (not gleemet.com). Zorova and Huslemad should NOT be clickable links.

**Robots Verification (SEO-07):**
7. Open `http://localhost:3000/robots.txt` -- should show `Allow: /` and sitemap URL, NO disallow rules

**Core Web Vitals Measurement (SEO-06, D-11):**
8. Open Chrome DevTools > Lighthouse tab
9. Run Lighthouse on `http://localhost:3000` (Performance category, Desktop) -- note LCP, CLS, INP scores
10. Run Lighthouse on `http://localhost:3000/case-studies/gleemeet` (Performance category, Desktop) -- note scores
11. Both pages should score green (LCP < 2.5s, CLS < 0.1, INP < 200ms)

**If CWV scores are NOT green, report the specific metric and value so Claude can apply the appropriate fix:**

- **LCP > 2.5s (red/orange):** Report the LCP value and which element Lighthouse identifies as the LCP element. Claude will fix by: (a) adding `priority` prop to the hero image/component if it is the LCP element, (b) verifying font preloading via `next/font` is working, (c) checking for render-blocking CSS or JS, (d) ensuring no large unoptimized images are loading above-the-fold.

- **CLS > 0.1 (red/orange):** Report the CLS value and which elements Lighthouse flags as shifting. Claude will fix by: (a) adding explicit `width` and `height` to any `<Image>` components missing dimensions, (b) adding CSS `aspect-ratio` or explicit sizing to containers that shift during load, (c) ensuring fonts have `font-display: swap` with matching fallback metrics (already handled by `next/font`).

- **INP > 200ms (red/orange):** Report the INP value and which interaction is slow. Claude will fix by: (a) checking for synchronous JS blocking the main thread during interactions, (b) verifying GSAP animations are not causing long tasks on scroll events, (c) deferring non-critical scripts (analytics, GTM) with `afterInteractive` strategy.

**Expected CWV results:**
- LCP should be improved with image optimization re-enabled
- CLS should remain low (no layout shifts -- images are SVG/inline, no external images loading)
- INP should be low (minimal interactive elements beyond scroll)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. If CWV scores are not green, report: "{metric}: {value} on {page} — LCP element: {element}" (e.g., "LCP: 3.1s on landing page — LCP element: hero heading"). Claude will apply targeted fixes and re-verify. For other issues: describe them (e.g., "OG image missing title", "Zorova is still clickable").</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. User confirms OG images render with correct accent colors
3. User confirms JSON-LD shows Organization type with founders
4. User confirms CWV scores are green (or non-green scores have been fixed per D-11)
5. User confirms dead links are non-clickable and live links work correctly
</verification>

<success_criteria>
- Full build passes with all Phase 5 changes
- User has verified OG images, JSON-LD, metadata, dead links, robots.txt, and CWV scores
- CWV scores are green (LCP < 2.5s, CLS < 0.1, INP < 200ms) on both landing and case study pages, OR non-green scores have been remediated (per D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-verification/05-03-SUMMARY.md`
</output>
