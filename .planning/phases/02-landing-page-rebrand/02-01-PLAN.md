---
phase: 02-landing-page-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Hero.tsx
  - app/layout.tsx
  - components/Navigation.tsx
autonomous: true
requirements: [STUD-01, STUD-09]

must_haves:
  truths:
    - "Hero section displays studio identity with two-line placeholder name, 'DEV STUDIO' scramble role, and studio-focused description"
    - "Hero stats show '6+' Products Shipped, '3+' Years Building Together, '2' Founders instead of personal stats"
    - "Page title reads 'Dev Studio' placeholder, not 'Abhishek Vaghela'"
    - "Navigation items list Team, Journey, How We Work, Projects, Case Studies, Contact"
    - "Scroll-spy array includes methodology section between experience and projects"
  artifacts:
    - path: "components/Hero.tsx"
      provides: "Studio-branded hero with preserved CharLine/scramble/blob/counter animations"
      contains: "Dev Studio"
    - path: "app/layout.tsx"
      provides: "Studio metadata in title, OG tags, Twitter cards, JSON-LD name/description"
      contains: "Dev Studio"
    - path: "components/Navigation.tsx"
      provides: "Updated nav items with new section names and scroll-spy array"
      contains: "methodology"
  key_links:
    - from: "components/Hero.tsx"
      to: "CharLine component"
      via: "same inline component, text prop changed"
      pattern: "CharLine text=\"Dev\""
    - from: "components/Navigation.tsx"
      to: "section IDs"
      via: "scroll-spy array + items array"
      pattern: "contact.*projects.*methodology.*experience.*team"
---

<objective>
Rebrand the Hero section, site metadata, and Navigation for studio identity.

Purpose: Establish the "two devs, one studio" positioning across the site's most visible touchpoints — the hero visitors see first, the metadata search engines index, and the navigation that appears on every page.
Output: Updated Hero.tsx with studio content (animations preserved), layout.tsx with studio metadata, Navigation.tsx with new section labels and scroll-spy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page-rebrand/02-CONTEXT.md
@.planning/phases/02-landing-page-rebrand/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/gsap.ts:
```typescript
export { gsap, ScrollTrigger, useGSAP };
```

From data/types.ts:
```typescript
export interface TeamMember {
  name: string;
  role: string;
  bio: string;
  github: string;
  linkedin: string;
  email: string;
  photo?: string;
  skills: string[];
}
```

From data/team.ts:
```typescript
export const team: TeamMember[] = [
  { name: 'Abhishek Vaghela', role: 'Full Stack Developer', email: 'vaghelaabhishek2580@gmail.com', github: 'https://github.com/abhishekvaghela', linkedin: 'https://linkedin.com/in/abhishekvaghela', ... },
  { name: 'Vatsal Zinzuvadiya', role: 'Full Stack Developer', email: 'vatsalzinzuvadiya@gmail.com', github: 'https://github.com/vatsalzinzuvadiya', linkedin: 'https://linkedin.com/in/vatsalzinzuvadiya', ... },
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand Hero section for studio identity</name>
  <files>components/Hero.tsx</files>
  <read_first>
    - components/Hero.tsx (current personal hero — understand CharLine, scramble, blob parallax, counter patterns)
    - .planning/phases/02-landing-page-rebrand/02-CONTEXT.md (decisions D-01 through D-04)
    - .planning/phases/02-landing-page-rebrand/02-RESEARCH.md (Hero Rewrite Pattern section, Pitfall 2 about CharLine character count)
  </read_first>
  <action>
Rewrite Hero.tsx content while preserving ALL existing animation code (CharLine, scramble, blob parallax, counter animations, marquee, scroll-linked effects). Changes are text/data only — no structural JSX or GSAP modifications.

Per D-01 (professional and clean tone), D-02 (studio metrics), D-03 (keep animations, change text only), D-04 (placeholder studio name):

1. **CharLine text props** — Change from personal name to studio placeholder:
   - Line 1: `<CharLine text="Dev" ...>` (was "Abhishek")
   - Line 2: `<CharLine text="Studio" ...>` (was "Vaghela")
   - sr-only span: `<span className="sr-only">Dev Studio — We Build Scalable Products</span>` (was "Abhishek Vaghela — Full Stack Developer")
   - Note: "Dev" (3 chars) and "Studio" (6 chars) are similar length to "Abhishek" (8) and "Vaghela" (7), keeping animation timing safe per Pitfall 2.

2. **Role scramble** — Change scramble target text:
   - `scramble(roleRef.current, 'DEV STUDIO');` (was 'FULL STACK DEVELOPER')
   - The static text in the roleRef div should also update to `DEV STUDIO` (this is the default visible text before JS runs)

3. **Description paragraph** (descRef) — Replace personal copy with studio copy:
   - New text: `"Two full-stack developers building scalable products together. From MVPs to production systems — we ship clean, performant code that grows with your business."`
   - This fits the "professional & clean" tone per D-01.

4. **Availability badge** — Update text:
   - `"Available for projects"` (was "Available for work")

5. **Stats counters** — Rename state variables and update targets per D-02:
   - `const [productsCount, setProductsCount] = useState(0);` (was corpCount)
   - `const [yearsCount, setYearsCount] = useState(0);` (was freeCount)
   - `const [foundersCount, setFoundersCount] = useState(0);` (was projCount)
   - Counter animation targets:
     - productsCount: target 6, increment by 1, interval 38ms, delay 1600ms. Display: `{productsCount}` (integer, no decimal)
     - yearsCount: target 3, increment by 0.1, interval 28ms, delay 1800ms. Display: `{yearsCount.toFixed(0)}` (integer)
     - foundersCount: target 2, increment by 1, interval 50ms, delay 2000ms. Display: `{foundersCount}` (integer)
   - Stats labels:
     - `"Products Shipped"` with violet accent `+` (was "Yrs Corporate")
     - `"Yrs Building Together"` with pink accent `+` (was "Yrs Freelance")
     - `"Founders"` with cyan accent (no `+` suffix for founders since it's exactly 2) (was "Projects")

6. **CTA buttons** (Claude's discretion per CONTEXT.md):
   - Primary: `"View Our Work"` linking to `#projects` (was "See my work")
   - Secondary: `"Get in Touch"` linking to `#contact` (was "Let's talk")

7. **Scroll link** at bottom:
   - `href="#team"` (was "#about") — matches new section ID for About/Team section

8. **Add TODO comment** at top of file: `// TODO: Replace "Dev Studio" with final studio name when decided`

Do NOT change:
- CharLine component definition
- scramble function
- blob refs or parallax logic
- GSAP timeline structure or timing offsets
- useGSAP scope or dependencies
- marquee content (STACK array stays — it's tech-focused, not personal)
- Any className values
  </action>
  <verify>
    <automated>cd D:/portfolio/companyportfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/Hero.tsx contains `<CharLine text="Dev"` (line must exist)
    - components/Hero.tsx contains `<CharLine text="Studio"` (line must exist)
    - components/Hero.tsx contains `scramble(roleRef.current, 'DEV STUDIO')` (exact string)
    - components/Hero.tsx contains `productsCount` and `setProductsCount` (renamed from corpCount)
    - components/Hero.tsx contains `yearsCount` and `setYearsCount` (renamed from freeCount)
    - components/Hero.tsx contains `foundersCount` and `setFoundersCount` (renamed from projCount)
    - components/Hero.tsx contains `Products Shipped` (stat label)
    - components/Hero.tsx contains `Yrs Building Together` (stat label)
    - components/Hero.tsx contains `Founders` (stat label)
    - components/Hero.tsx contains `href="#team"` (scroll link target)
    - components/Hero.tsx contains `TODO: Replace "Dev Studio"` (placeholder comment)
    - components/Hero.tsx contains `{ scope: sectionRef` (GSAP scope preserved)
    - components/Hero.tsx does NOT contain `Abhishek` (personal name removed)
    - components/Hero.tsx does NOT contain `corpCount` (old variable name removed)
    - tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Hero displays studio identity with "Dev Studio" placeholder name, DEV STUDIO scramble role, studio description, and 3 studio metrics (6+ products, 3+ years, 2 founders). All animations preserved unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update site metadata for studio branding</name>
  <files>app/layout.tsx</files>
  <read_first>
    - app/layout.tsx (current personal metadata — understand metadata export structure, JSON-LD object, OG tags)
    - .planning/phases/02-landing-page-rebrand/02-CONTEXT.md (decision D-19)
    - .planning/phases/02-landing-page-rebrand/02-RESEARCH.md (Pitfall 6 — JSON-LD @type must stay Person until Phase 5)
  </read_first>
  <action>
Update all metadata text strings in layout.tsx from personal branding to studio branding per D-19. CRITICAL: Do NOT change `'@type': 'Person'` in jsonLd — schema type migration is Phase 5 (SEO-01).

1. **metadata export** — Update these fields:
   - `title`: `'Dev Studio — Full Stack Development Team | React, Node.js, TypeScript'`
   - `description`: `'Two full-stack developers building scalable web applications. Expertise in React, Next.js, Node.js, TypeScript, MongoDB, AWS. Available for projects and collaboration.'`
   - `keywords`: Replace personal keywords. New array:
     ```
     ['Dev Studio', 'Full Stack Development Team', 'React Developers', 'Node.js Development', 'TypeScript Experts', 'Next.js Development', 'MVP Development', 'Web Application Development', 'Scalable Products', 'MongoDB', 'AWS', 'Docker']
     ```
   - `authors`: `[{ name: 'Dev Studio', url: 'https://abhishekvaghela.dev' }]` (URL stays — domain unchanged)
   - `creator`: `'Dev Studio'`
   - `publisher`: `'Dev Studio'`
   - `openGraph.title`: `'Dev Studio — Full Stack Development Team'`
   - `openGraph.description`: `'Two full-stack developers building scalable web apps with React, Node.js, and modern technologies.'`
   - `openGraph.siteName`: `'Dev Studio'`
   - `openGraph.images[0].alt`: `'Dev Studio - Full Stack Development Team'`
   - `twitter.title`: `'Dev Studio — Full Stack Development Team'`
   - `twitter.description`: `'Two full-stack developers. React, Node.js, TypeScript, AWS.'`

2. **jsonLd object** — Update text fields ONLY (NOT @type):
   - `name`: `'Dev Studio'`
   - `jobTitle`: `'Full Stack Development Studio'`
   - Keep `url`, `email`, `telephone` unchanged (still Abhishek's as primary contact)
   - `sameAs`: Keep both GitHub/LinkedIn links unchanged
   - `worksFor`: Remove entirely (studio IS the entity, not employed by someone)
   - `alumniOf`: Keep unchanged (still accurate — founder is a GEC Modasa alum)
   - `knowsAbout`: Keep unchanged

3. **Add TODO comment** above metadata export: `// TODO: Replace "Dev Studio" with final studio name when decided`

Do NOT change:
- Font loading (inter, jetbrainsMono, flagfies)
- GTM script
- SmoothScrollProvider / AnalyticsProvider wrapping
- `metadataBase` URL
- `viewport` export
- `robots` config
- JSON-LD `'@type': 'Person'` — this stays until Phase 5
  </action>
  <verify>
    <automated>cd D:/portfolio/companyportfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx contains `title: 'Dev Studio` (metadata title starts with Dev Studio)
    - app/layout.tsx contains `creator: 'Dev Studio'`
    - app/layout.tsx contains `publisher: 'Dev Studio'`
    - app/layout.tsx contains `siteName: 'Dev Studio'`
    - app/layout.tsx contains `name: 'Dev Studio'` (in jsonLd)
    - app/layout.tsx contains `'@type': 'Person'` (NOT changed to Organization — Phase 5)
    - app/layout.tsx contains `TODO: Replace "Dev Studio"` (placeholder comment)
    - app/layout.tsx does NOT contain `creator: 'Abhishek Vaghela'` (personal branding removed from creator)
    - app/layout.tsx does NOT contain `title: 'Abhishek Vaghela` (personal title removed)
    - tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>All site metadata (title, description, OG tags, Twitter cards, JSON-LD name/description) reflects "Dev Studio" placeholder branding. JSON-LD @type remains Person for Phase 5 migration.</done>
</task>

<task type="auto">
  <name>Task 3: Update Navigation for new section labels and scroll-spy</name>
  <files>components/Navigation.tsx</files>
  <read_first>
    - components/Navigation.tsx (current nav items array, scroll-spy array, social links, logo)
    - .planning/phases/02-landing-page-rebrand/02-RESEARCH.md (Section Numbering table, Pitfall 3 about scroll-spy array order)
    - .planning/phases/02-landing-page-rebrand/02-CONTEXT.md (D-13 for page flow order)
  </read_first>
  <action>
Update Navigation.tsx for the new section structure per D-13 page flow: Hero -> Team (About) -> Experience (Journey) -> How We Work -> Projects -> Contact.

1. **items array** — Replace the current array with:
   ```typescript
   const items = [
     { name: 'Team', href: '#team' },
     { name: 'Journey', href: '#experience' },
     { name: 'How We Work', href: '#methodology' },
     { name: 'Projects', href: '#projects' },
     { name: 'Case Studies', href: '/case-studies' },
     { name: 'Contact', href: '#contact' },
   ];
   ```
   Note: "About" becomes "Team", "Experience" stays as href but label becomes "Journey", "How We Work" is new with href `#methodology`.

2. **Scroll-spy array** (in the homepage scroll-spy useEffect) — Update from:
   `['contact', 'projects', 'experience', 'about']`
   to:
   `['contact', 'projects', 'methodology', 'experience', 'team']`
   This is reverse DOM order per the existing pattern. The `methodology` ID is inserted between `projects` and `experience`.

3. **Logo aria-label** — Update from `"Abhishek Vaghela — Home"` to `"Dev Studio — Home"`.

4. **Add TODO comment** near logo: `// TODO: Replace "Dev Studio" with final studio name when decided`

Do NOT change:
- usePathname / useRouter usage
- handleNavClick logic (route-aware hash navigation)
- Social links in header (GitHub, LinkedIn, Mail) — keep Abhishek's links for now
- Mobile menu structure or toggle animation
- scrolled state logic
- Case Studies active state logic (pathname.startsWith check)
  </action>
  <verify>
    <automated>cd D:/portfolio/companyportfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/Navigation.tsx contains `{ name: 'Team', href: '#team' }` (first nav item)
    - components/Navigation.tsx contains `{ name: 'Journey', href: '#experience' }` (experience nav item)
    - components/Navigation.tsx contains `{ name: 'How We Work', href: '#methodology' }` (new nav item)
    - components/Navigation.tsx contains `'contact', 'projects', 'methodology', 'experience', 'team'` (scroll-spy array, reverse DOM order)
    - components/Navigation.tsx contains `Dev Studio` in aria-label (logo)
    - components/Navigation.tsx does NOT contain `{ name: 'About'` (old nav item)
    - components/Navigation.tsx does NOT contain `'about'` in scroll-spy array
    - tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Navigation shows Team, Journey, How We Work, Projects, Case Studies, Contact links. Scroll-spy correctly tracks all 5 homepage sections including the new methodology section. Logo aria-label uses studio name.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully
3. Hero.tsx contains no personal branding (no "Abhishek", no "corpCount", no "freeCount")
4. layout.tsx metadata all references "Dev Studio" placeholder
5. Navigation.tsx items array has 6 entries including "How We Work"
6. Navigation scroll-spy includes "methodology" and "team" (not "about")
</verification>

<success_criteria>
- The hero section reads as a studio landing, not a personal portfolio
- Site metadata (title, OG, Twitter) reflects "Dev Studio" branding
- Navigation labels match the new section structure with no orphan links
- All GSAP animations in Hero continue to work unchanged (CharLine, scramble, blobs, counters, parallax)
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page-rebrand/02-01-SUMMARY.md`
</output>
